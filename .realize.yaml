settings:
  legacy:
    force: false
    interval: 0s
schema:
- name: docker-golang-develop-skeleton
  path: .
  commands:
    run:
      status: true
  watcher:
    extensions:
    - go
    - html
    - yaml
    paths:
    - /
    ignored_paths:
    - .git
    - .realize
    - vendor
