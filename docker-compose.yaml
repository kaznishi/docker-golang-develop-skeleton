version: '3'
services:
  golang:
    build:
      context: .
      dockerfile: docker/golang/Dockerfile
    volumes:
      - .:/go/src/kaznishi/docker-golang-develop-skeleton
    ports:
      - 8080:8080
